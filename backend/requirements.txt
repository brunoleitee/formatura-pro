fastapi==0.115.6
uvicorn==0.34.0
opencv-python==4.10.0.84
numpy==1.26.4
pydantic==2.10.4
Pillow==11.1.0
rawpy==0.21.0
# DirectML: funciona com qualquer GPU Windows (NVIDIA, AMD, Intel) sem CUDA
onnxruntime-directml==1.20.1
insightface==0.7.3
faiss-cpu==1.8.0
pydantic-settings==2.7.0
python-multipart==0.0.20
starlette>=0.40.0,<0.42.0
typing-extensions==4.12.2
transformers==4.48.3
huggingface_hub==0.27.1
reportlab>=4.0.0
google-auth-oauthlib>=1.4.0
google-api-python-client>=2.196.0
psutil>=6.0.0

# ── OCR Moderno ──
# PaddleOCR + PaddlePaddle (CPU ou GPU via CUDA)
# Uso: pip install paddlepaddle paddleocr
# GPU: pip install paddlepaddle-gpu paddleocr
# Alternativa leve: remover paddlepadded e usar docTR ou Surya OCR.
# PaddleOCR funciona offline após primeiro download dos modelos.

# ── VLM Contextual (Qwen2.5-VL) ──
# transformers >= 4.48.0 já incluído acima
# torch >= 2.0 (necessário para Qwen e PaddleOCR GPU)
# Instalação: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# qwen-vl-utils: pip install qwen-vl-utils
